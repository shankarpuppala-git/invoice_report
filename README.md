# invoice_report
FastAPI service that generates invoice reports — fetches credit-card orders from PostgreSQL, resolves transaction statuses via Authorize.net, and returns a formatted Excel workbook on demand.
